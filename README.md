# SumoPerception
teaching/assignment toolkit for converting **SUMO traffic simulations** into **MATLAB-friendly traces** and running a **360° vehicle perception algorithm** (with optional occlusion/masking) to compute what each vehicle “detects” at every sensing cycle.
